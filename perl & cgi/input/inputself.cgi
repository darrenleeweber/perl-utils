#!/usr/local/bin/perl

$rate = 55;
$distance = 400;
$driver = "Marion";
$destination = "42 Indy Place";

$name = $driver;

print "Content-type: text/html\n\n";
print "The rate is $rate, the distance is $distance, the driver is
$driver, the destination is $destination. And the driver's name really is
$name.";

